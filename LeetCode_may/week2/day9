class Solution {
public:
    bool isPerfectSquare(int num) {
        if(num==1)
            return true;
        for (long long sum = 0, i = 1; sum < num; i += 2) { 
        sum += i; 
        if (sum == num) 
            return true; 
    } 
    return false; 
    }
};